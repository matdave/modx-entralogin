name: Service
version: 0.0.1
lowCaseName: entralogin
namespace: Service
description: 'Entra Login for MODX Revolution'
author: matdave
systemSettings:
    - key: client_id
      area: API

    - key: client_secret
      area: API

    - key: auth_code
      area: API

    - key: tenant_id
      area: API
      value: common

    - key: allowed_hosts
      area: API
      value: graph.microsoft.com
